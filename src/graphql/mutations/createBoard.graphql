mutation CreateBoard($title:String!, $projectId:String!){
  createBoard(title:$title, projectId:$projectId){
    boards{
      id
      title
      boardColumnIndex
    }
    error{
      message
      code
      field
    }
  }
}