mutation CreateProject($name: String!) {
	createProject(name:$name) {
			project {
					id
					name
			}
			error {
					code
					message
					field
			}
			project {
					id
					name
			}
			error {
					code
					message
					field
			}
	}
}